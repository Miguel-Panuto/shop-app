import React from 'react';
import { View, Text } from 'react-native';

export const ManageScreen = props => {
    return(
        <View>
            <Text>Manage Screen</Text>
        </View>
    );
};

export default ManageScreen;